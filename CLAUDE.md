# 源 Minamoto (etymology-drops)

英単語を語源（部品＝morpheme）に分解して学ぶ Next.js アプリ。UI コピーは日本語がデフォルト（`src/locales/ja.ts` が正、`en.ts` を同期）。

## Writing rules（必ず守る）

- **日本語の文章に em dash（— / ——）を使わない。** 区切りには「。」「、」「？」「＝」やカギ括弧を使う。英語コピーでの em dash は可。
